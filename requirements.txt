# Core dependencies for ProofTrace FaceChain
# Note: FaceChain includes pure-Python standard library fallbacks if OpenCV or Pillow are unavailable.

# Optional but recommended for YuNet / Haar face localization
opencv-python-headless>=4.8.0.76
numpy>=1.24.0

# Optional for perceptual resampling
pillow>=10.0.0

# Standard HTTP client
urllib3>=2.0.0
